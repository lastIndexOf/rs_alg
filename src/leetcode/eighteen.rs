pub struct Solution;

impl Solution {
    pub fn four_sum(nums: Vec<i32>, target: i32) -> Vec<Vec<i32>> {
        vec![]
    }
}

#[cfg(test)]
mod eighteen_test {
    use super::*;

    #[test]
    fn test_eighteen() {}
}
